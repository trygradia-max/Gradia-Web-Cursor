/**
 * Client-side entry: use `createBrowserSupabaseClient()` from `@/lib/supabase/client`
 * in client components. This file re-exports for a short import path.
 */
export { createBrowserSupabaseClient } from "./supabase/client";
